# Secrets

Place deployment secrets here. For Streamlit Cloud, copy `secrets.example.toml` to `.streamlit/secrets.toml` inside the `streamlit/` directory with your real values.

Sensitive files in this folder should not be committed; keep only sanitized templates.
